# 🧼 Trusted Types Sanitizer | XSS Defense

### 🇧🇷 Motor de Higienização de Scripts
Implementação da API de **Trusted Types**. Este módulo bloqueia qualquer atribuição ao `innerHTML` ou `outerHTML` que não tenha passado por uma política de sanitização pré-definida, eliminando ataques de XSS por design.

---

### 🇺🇸 Script Sanitization Engine
Implementing the **Trusted Types API**. This module blocks any assignment to `innerHTML` or `outerHTML` that hasn't passed through a predefined sanitization policy, eliminating XSS attacks by design.
