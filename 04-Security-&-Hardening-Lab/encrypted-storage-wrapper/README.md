# 🔐 Encrypted Storage | Data-at-Rest Protection

### 🇧🇷 Blindagem de Armazenamento Local
Abstração para `LocalStorage` e `SessionStorage` que utiliza criptografia simétrica para armazenar dados sensíveis. Garante que, mesmo que o storage seja exportado, os dados permaneçam ilegíveis sem a chave de sessão.

---

### 🇺🇸 Local Storage Hardening
Abstraction for `LocalStorage` and `SessionStorage` using symmetric encryption to store sensitive data. Ensures that even if the storage is exported, data remains unreadable without the session key.
