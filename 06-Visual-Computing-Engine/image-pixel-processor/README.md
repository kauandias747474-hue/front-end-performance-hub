# 📷 Image Pixel Processor | Real-time Filters

### 🇧🇷 Processamento de Imagem em Nível de Pixel
Um mini-editor que aplica filtros (ex: escala de cinza, sépia, inversão) em tempo real diretamente nos pixels de uma imagem. Utiliza `ImageData` e a API Canvas para manipulação de arrays de bytes da imagem.

---

### 🇺🇸 Pixel-Level Image Processor
A mini-editor that applies filters (e.g., grayscale, sepia, invert) in real-time directly to the pixels of an image. Uses `ImageData` and the Canvas API for byte array image manipulation.
