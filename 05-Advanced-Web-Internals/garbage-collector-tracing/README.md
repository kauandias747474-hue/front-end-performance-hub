# ♻️ Memory Lifecycle Audit | WeakRef Tracking

### 🇧🇷 Monitoramento de Ciclo de Vida de Objetos
Pesquisa focada em prevenir *Memory Leaks* silenciosos. Utilizo `WeakRef` e `FinalizationRegistry` para auditar se objetos de grande porte estão sendo liberados corretamente pelo Garbage Collector sem manter referências fortes acidentais.

---

### 🇺🇸 Object Lifecycle Monitoring
Research focused on preventing silent memory leaks. I use `WeakRef` and `FinalizationRegistry` to audit if large objects are being correctly released by the Garbage Collector without keeping accidental strong references.
