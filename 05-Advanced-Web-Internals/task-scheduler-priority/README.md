# ⏳ Task Scheduler | UI Interactivity

### 🇧🇷 Escalonamento de Tarefas Não-Bloqueantes
Pesquisa sobre a priorização de mensagens no navegador. Utilização de `requestIdleCallback` e `postMessage` para fragmentar processamentos pesados em pequenos pedaços, garantindo que a UI responda a 60 FPS.

---

### 🇺🇸 Non-Blocking Task Scheduling
Research on browser message prioritization. Using `requestIdleCallback` and `postMessage` to fragment heavy processing into small chunks, ensuring the UI remains responsive at 60 FPS.
