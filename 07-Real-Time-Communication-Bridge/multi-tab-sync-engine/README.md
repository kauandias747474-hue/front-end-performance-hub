# 🔄 Multi-Tab Sync | Cross-Instance Integrity

### 🇧🇷 Sincronização entre Instâncias
Utiliza `BroadcastChannel` para sincronizar o estado da aplicação entre múltiplas abas do navegador. Garante que atualizações de tempo real recebidas em uma instância sejam propagadas para as demais sem redundância de tráfego de rede.

---

### 🇺🇸 Cross-Instance Synchronization
Uses `BroadcastChannel` to sync application state across multiple browser tabs. Ensures real-time updates received in one instance are propagated to others without network traffic redundancy.
